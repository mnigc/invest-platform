# 同步脚本依赖
# 安装：pip install -r requirements.txt
# 数据库连接：导出环境变量 DATABASE_URL（Supabase Session Pooler 连接串）
#   postgresql://postgres.<project-ref>:<password>@aws-0-<region>.pooler.supabase.com:5432/postgres

# ===== 数据库 =====
psycopg2-binary>=2.9.9

# ===== 金融数据源 =====
yfinance>=0.2.40           # 美股/ETF/A股主要指数 —— 海外直连稳定
requests>=2.31.0           # FRED 等 REST API
akshare>=1.18.64           # A 股指数 / 中国宏观 / 国债 / 北向资金 / PMI —— 海外环境建议配置 HTTP_PROXY 回国内

# ===== 通用 =====
pandas>=2.0.0              # 所有脚本均用 pandas 处理返回

# ===== 可选 =====
python-dotenv>=1.0.0       # 允许用 .env 文件加载环境变量（不装也行，直接 export）
# tushare>=1.2.89           # 如需使用 Tushare Pro 作为中国数据备用源（需 TUSHARE_TOKEN）
