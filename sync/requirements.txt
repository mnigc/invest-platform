# 同步脚本依赖
# 安装：pip install -r requirements.txt
# 数据库连接：导出环境变量 DATABASE_URL（Supabase Session Pooler 连接串）
#   postgresql://postgres.<project-ref>:<password>@aws-0-<region>.pooler.supabase.com:5432/postgres

# ===== 数据库 =====
psycopg2-binary>=2.9.9

# ===== 金融数据源 =====
# 按展示模块所需：
#   yfinance -> 美元指数 DXY（黄金决策）、沪深300 日线兜底（国家队资金）
#   FRED     -> 美债/宏观/实际利率/流动性（宏观体制、全球流动性、黄金决策）
yfinance>=0.2.40           # 美元指数 / 指数日线兜底 —— 海外直连稳定
curl_cffi>=0.7.0           # Yahoo 浏览器伪造直连（境外 / GHA 绕过限流，金价历史 + DXY 主用）
requests>=2.31.0           # FRED REST API、gold-api.com

# ===== 通用 =====
pandas>=2.0.0              # 处理返回数据

# ===== 可选 =====
python-dotenv>=1.0.0       # 允许用 .env 文件加载环境变量（不装也行，直接 export）
# tushare>=1.2.89           # 如需使用 Tushare Pro 作为中国数据备用源（需 TUSHARE_TOKEN）
